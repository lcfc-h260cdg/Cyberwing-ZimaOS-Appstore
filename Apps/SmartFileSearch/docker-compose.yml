name: smart-file-search

services:
  llm_server:
    image: docker-cyberwing-registry.cn-hangzhou.cr.aliyuncs.com/cyberwing/llm_server:v2.1.2
    container_name: llm_server
    restart: always
    volumes:
      - /DATA:/DATA:rslave
      - /media:/media:rslave

      - type: bind
        source: /DATA/AppData/AI_MODELS/llm-server
        target: /data/models
        bind:
          create_host_path: true
      - type: bind
        source: /tmp
        target: /tmp
        bind:
          create_host_path: true
    environment:
      LLM_CPU_PINNING: "true"
      LLM_CPU_THREADS: "4"
      LLM_OV_NUM_STREAMS: "1"
      MODEL_ROOT: /data/models
      TOKENIZERS_PARALLELISM: "false"
      TZ: Asia/Shanghai
    network_mode: host
    privileged: true
    command:
      - /app/llm_server
    entrypoint: []
    devices:
      - /dev/dri:/dev/dri
    # 实测高负载约 10.2 GiB，给 11 GiB 硬上限；原配置没有任何边界。
    mem_limit: "13g"
    mem_reservation: "10g"
    memswap_limit: "13g"
    deploy:
      resources:
        limits:
          memory: "13g"
        reservations:
          memory: "10g"
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "3"
        compress: "true"

  pgvector-nas:
    image: docker-cyberwing-registry.cn-hangzhou.cr.aliyuncs.com/cyberwing/pgvector/pgvector:pg18-trixie
    container_name: pgvector-nas
    restart: always
    ports:
      - "5434:5432"
    volumes:
      - /DATA/AppData/LCFC_APP/data/ai_api_data/pgvector_nas_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: lcfc
      POSTGRES_PASSWORD: lcfc
      POSTGRES_DB: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
      TZ: Asia/Shanghai
    mem_limit: "1536m"
    mem_reservation: "512m"
    memswap_limit: "1536m"
    deploy:
      resources:
        limits:
          memory: "1536m"
        reservations:
          memory: "512m"
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "3"
        compress: "true"

  file-search-service:
    image: docker-cyberwing-registry.cn-hangzhou.cr.aliyuncs.com/cyberwing/file-rag-nas:v2.0.0_20260706
    container_name: file-search-service
    restart: always
    volumes:
      # User file roots. Keep original host paths inside the container.
      - /DATA:/DATA:rslave
      - /media:/media:rslave

      # Service state and config.
      - /DATA/AppData/LCFC_APP/data/ai_api_data/file-search-service/parsing_service:/data/ai-core/parsing_service
      - /DATA/AppData/LCFC_APP/data/ai_api_data/file-search-service/config:/NAS_Route/config

      # Service logs.
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/file-search-service/parsing_service:/RagBaseHaystack_svn/parsing_log
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/file-search-service/document_analysis:/RagBaseHaystack_svn/data_loader/RagDocumentAnalysis/log
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/file-search-service/nas_query:/data/ai-core/nas_query/logs
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/file-search-service/nas_retrieval:/Nas_Retrieval_Final_031/logs
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/file-search-service/nas_route:/NAS_Route/logs
    environment:
      PYTHONUNBUFFERED: "1"
      LC_ALL: C.UTF-8
      LANG: C.UTF-8
      FLASK_ENV: production
      FLASK_DEBUG: "false"
      HOST: 0.0.0.0
      PORT: "20194"
      TZ: Asia/Shanghai
      OPENAI_API_KEY: sk-abcdefg0123456789abcdefg
      OPENAI_BASE_URL: https://dashscope.aliyuncs.com/compatible-mode/v1
      OPENAI_MODEL: qwen-plus
      RAG_TEMPERATURE: "0"
      GENERATION_TEMPERATURE: "0.7"
      MODEL_NAME: OpenAI
      NAS_ROUTE_PORT: "20191"
      CONFIG_DIR: /NAS_Route/config
      LOG_DIR: /NAS_Route/logs
      AUTO_INIT_CONFIG: "true"
    network_mode: host
    healthcheck:
      test:
        - CMD
        - sh
        - -c
        - curl -f http://localhost:20194/health && curl -f http://localhost:20193/health && curl -f http://localhost:20192/docs && curl -f http://localhost:20191/health
      interval: 60s
      timeout: 15s
      retries: 5
      start_period: 120s
    command:
      - sh
      - -c
      - /start.sh
    # 正常解析实测约 3.9 GiB；异常文件到 4.5 GiB 即在本容器内失败，
    # 避免再次吞完宿主机内存和 Swap。
    mem_limit: "4608m"
    mem_reservation: "3g"
    memswap_limit: "4608m"
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: "4608m"
        reservations:
          memory: "3g"
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "3"
        compress: "true"

  asr-server:
    image: docker-cyberwing-registry.cn-hangzhou.cr.aliyuncs.com/cyberwing/asr-server:devel-20260721
    container_name: asr-server
    restart: always
    ports:
      - "20105:8000"
    volumes:
      # ASR needs to read media files.
      - /DATA:/DATA:rslave
      - /media:/media:rslave

      # ASR model and logs.
      - /DATA/AppData/AI_MODELS/asr-server/funasr_models:/app/funasr_models
      - /DATA/AppData/LCFC_APP/data/ai_api_data/asr-server/logs:/app/logs
      - /usr/share/zoneinfo/Asia/Shanghai:/usr/share/zoneinfo/Asia/Shanghai
      - /etc/timezone:/etc/timezone
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
      PYTHONUTF8: "1"
      LANG: C.UTF-8
      LC_ALL: C.UTF-8
      TZ: Asia/Shanghai
    healthcheck:
      test:
        - CMD
        - curl
        - -f
        - http://localhost:8000/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    working_dir: /app/asr_src
    mem_limit: "1536m"
    mem_reservation: "768m"
    memswap_limit: "1536m"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: "1536m"
        reservations:
          memory: "768m"
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "3"
        compress: "true"

  ai-api:
    image: docker-cyberwing-registry.cn-hangzhou.cr.aliyuncs.com/cyberwing/ai-api:devel-20260803
    container_name: ai-api
    restart: always
    depends_on:
      - llm_server
      - pgvector-nas
      - asr-server
      - file-search-service
    volumes:
      # API service data.
      - /DATA/AppData/LCFC_APP/data/ai_api_data/data_base:/app/data/data_base
      - /DATA/AppData/LCFC_APP/data/ai_api_data/vector_db:/app/data/vector_db
      - /DATA/AppData/LCFC_APP/data/ai_api_data/vector_cache:/app/data/vector_cache
      - /DATA/AppData/LCFC_APP/data/ai_api_data/logs/ai-api:/app/data/logs
      - /DATA/AppData/LCFC_APP/data/ai_api_data/configs:/app/data/configs

      - /DATA/.config/secrets:/app/data/secrets
      # File watcher config. Keep both targets for old/new code compatibility.
      - type: bind
        source: /DATA/.config/rag/filewatchsettings.yaml
        target: /app/filewatchsettings.yaml
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: /DATA/.config/rag/filewatchsettings.yaml
        target: /filewatchsettings.yaml
        read_only: true
        bind:
          create_host_path: false

      # Shared model root.
      - /DATA/AppData/AI_MODELS/llm-server:/data/models

      # User file roots. Keep original host paths inside the container.
      - /DATA:/DATA:rslave
      - /media:/media:rslave

      # ILAB data.
      - /DATA/AppData/LCFC_APP/data/ai_api_data/ilab_data_base:/app/ilab/data/data_base
      - /DATA/AppData/LCFC_APP/data/ai_api_data/ilab_vector_db:/app/ilab/data/vector_db
      - /DATA/AppData/LCFC_APP/data/ai_api_data/ilab_milvus_data:/app/ilab/data/milvus_data
      - /DATA/AppData/LCFC_APP/data/ai_api_data/ilab_logs:/app/ilab/data/logs
      - /DATA/AppData/LCFC_APP/data/ai_api_data/ilab_config:/app/ilab/data/configs

      # Host system info required by the API.
      - /sys/class/dmi/id:/sys/class/dmi/id:ro
      - /opt:/opt
      - /tmp:/tmp
    environment:
      TZ: Asia/Shanghai
      FILE_WATCH_SETTINGS_PATH: /app/filewatchsettings.yaml
    network_mode: host
    privileged: true
    healthcheck:
      test:
        - CMD-SHELL
        - curl -f http://localhost:20100/docs && curl -f http://localhost:20190/docs || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # 运行期约 2.3 GiB；留出增长空间，但不再允许占满原来的 10 GiB。
    mem_limit: "4608m"
    mem_reservation: "3g"
    # 等于 mem_limit 表示该容器不使用宿主机 Swap，超限时在容器内OOM
    memswap_limit: "4608m"
    deploy:
      resources:
        limits:
          cpus: "10.0"
          memory: "4608m"
        reservations:
          memory: "3g"
    logging:
      driver: json-file
      options:
        max-size: 50m
        max-file: "3"
        compress: "true"

networks:
  default:
    name: smart-file-search_default

x-casaos:
  id: com.mycyberwing.smart-file-search
  main: ai-api
  icon: "https://zimaos-store.oss-cn-beijing.aliyuncs.com/SmartFileSearch/icon.png"
  title:
    zh_CN: 小宝检索服务
    en_US: "Xiaobao RAG Service"
  tagline:
    zh_CN: 小宝检索服务
    en_US: "RAG Service for Cyberwing Device"
  description:
    zh_CN: 小宝检索服务
    en_US: "RAG Service for Cyberwing Device"
  tips: {}
  author: LCFC
  developer: nmh
  category: AI
  architectures:
    - amd64
  version: "1.0.4"
  update_at: "2026-08-04"
  release_notes:
    en_US: |-
      - "2026-07-10 First release"
      - "2026-07-15 Update ai-api service"
      - "2026-07-22 Update RAG facial recognition"
      - "2026-07-24 Update LLM Server"
      - "2026-08-04 Optimize memory usage, increase memory limits, and add support for ASR Chinese path."
    zh_CN: |-
      - "2026-07-10 首次上线"
      - "2026-07-15 更新 ai-api 服务"
      - "2026-07-22 更新 RAG 人脸识别"
      - "2026-07-24 更新 LLM Server"
      - "2026-08-04 优化内存占用，增加内存限制，增加 ASR 中文路径支持"
